spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://s465z7sj4pwhp7fn.cbetxkdyhwsb.us-east-1.rds.amazonaws.com/reminder?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: pw3rhemgvfmruwgi
    password: hm8fa297t7bh8q3o

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace

server:
  port: ${port:8080}